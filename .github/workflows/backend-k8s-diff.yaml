name: Kubernetes Diff

on:
  pull_request:
    paths:
      - "k8s/**"
      - ".github/workflows/backend-k8s-diff.yaml"

permissions:
  id-token: write
  contents: read
  pull-requests: write

env:
  GCP_PROJECT_ID: devops-fmi-course-476112
  GCP_ZONE: europe-west4-a
  GCP_CLUSTER: devops-fmi-course-gke
  GCP_WORKLOAD_IDENTITY_PROVIDER: projects/632982591699/locations/global/workloadIdentityPools/github-actions-pool/providers/github-actions
  GCP_SA_EMAIL: github-terraform-deployer@devops-fmi-course-476112.iam.gserviceaccount.com

jobs:
  preview-diff:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Authenticate to GCP via OIDC
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ env.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ env.GCP_SA_EMAIL }}
          token_format: "access_token"
          project_id: ${{ env.GCP_PROJECT_ID }}

      - name: Set up gcloud CLI
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.GCP_PROJECT_ID }}
          install_components: gke-gcloud-auth-plugin

      - name: Enable GKE gcloud auth plugin
        run: echo "USE_GKE_GCLOUD_AUTH_PLUGIN=True" >> $GITHUB_ENV

      - name: Get GKE credentials
        run: |
          gcloud container clusters get-credentials "$GCP_CLUSTER" \
            --zone "$GCP_ZONE" \
            --project "$GCP_PROJECT_ID"

      - name: Install Kustomize
        uses: imranismail/setup-kustomize@v2.1.0
        with:
          kustomize-version: "*"

      - name: Render manifests and compute kubectl diff
        run: |
          cd k8s/prod
          kustomize build . > /tmp/rendered.yaml
          kubectl diff -f /tmp/rendered.yaml > /tmp/kubectl-diff.txt || true

      - name: Post Kubernetes diff as PR comment
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let diff = '';
            try {
              diff = fs.readFileSync('/tmp/kubectl-diff.txt', 'utf8');
            } catch (e) {
              diff = '';
            }

            let body;
            if (!diff.trim()) {
              body = '### Kubernetes diff\n\nNo changes detected for this PR.';
            } else {
              const truncated = diff.length > 60000
                ? diff.slice(0, 60000) + '\n...(truncated)'
                : diff;

              body = [
                '### Kubernetes manifest diff (preview)',
                '',
                '_This shows how the **cluster** would change if we applied the current `k8s/prod` manifests._',
                '',
                '```diff',
                truncated,
                '```'
              ].join('\n');
            }

            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body
            });
